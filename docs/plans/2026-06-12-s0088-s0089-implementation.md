# S0088 S0089 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete S0088 GUI streaming thinking and S0089 memory reliability/dream trigger.

**Architecture:** S0088 adds a minimal `thinking_delta` event that streams provider-produced thinking into the same in-flight assistant turn and reconciles with the final persistent assistant message. S0089 keeps the existing memory model but adds quality checks, persistent dream state, restart recovery, diagnostics, and a GUI status surface.

**Tech Stack:** TypeScript, Scorel protocol/core/daemon/client, React GUI, Vitest.

---

### Task 1: S0088 Protocol And Runtime

**Files:**
- Modify: `packages/protocol/src/events.ts`
- Modify: `packages/core/src/runtime/index.ts`
- Modify: `packages/core/src/provider/pi-ai.ts`
- Test: `packages/core/src/runtime/runtime.test.ts`
- Test: `packages/protocol/src/index.test.ts`

**Steps:**
1. Add `thinking_delta` to transient event types.
2. Extend `ProviderStreamChunk` and `RawRuntimeEvent` with `{ type: "thinking_delta"; delta: string }`.
3. Collect streamed thinking in runtime alongside text.
4. When provider returns no final message, synthesize both thinking and text blocks in order.
5. Teach the pi-ai provider to forward upstream thinking deltas when present.
6. Add tests for ordered streaming thinking and final assistant reconciliation.

### Task 2: S0088 Daemon And GUI Projection

**Files:**
- Modify: `packages/daemon/src/index.ts`
- Modify: `apps/gui/src/renderer/chatbox/projector.ts`
- Test: `packages/daemon/src/embedded/embedded.test.ts`
- Test: `apps/gui/src/session-event-lifecycle.test.ts` or nearby projector test

**Steps:**
1. Broadcast raw `thinking_delta` as a transient event using the current assistant event id.
2. Include thinking block counts in assistant result diagnostics.
3. Add projector handling that appends thinking deltas to a thinking part in the in-flight assistant turn.
4. Ensure final `assistant_message` replaces the streaming placeholder rather than duplicating it.
5. Add GUI projector tests for thinking before text and thinking reconciliation.

### Task 3: S0089 Memory Core Quality And State

**Files:**
- Modify: `packages/core/src/memory/index.ts`
- Test: `packages/core/src/memory/memory.test.ts`

**Steps:**
1. Add AppendDaily low-signal validation for empty/generic summaries and fully empty detail arrays.
2. Add duplicate detection against the current daily file.
3. Add project memory status helpers reading/writing `dream-state.json`.
4. Add tests for validation, duplicate rejection, and status serialization.

### Task 4: S0089 Daemon Dream Recovery And API

**Files:**
- Modify: `packages/protocol/src/events.ts`
- Modify: `packages/protocol/src/wire.ts`
- Modify: `packages/client/src/index.ts`
- Modify: `packages/daemon/src/index.ts`
- Test: `packages/daemon/src/embedded/embedded.test.ts`
- Test: `packages/client/src/daemon-client.test.ts` if client coverage is needed

**Steps:**
1. Add `MemoryStatus` and `get_memory_status`.
2. On AppendDaily success, persist dirty/scheduled dream state before setting the in-process timer.
3. On project/session use or memory status read, recover pending dirty dream state and schedule it.
4. Update dream state to running/succeeded/failed with timestamps and error text.
5. Add daemon tests for status, restart recovery, and failure visibility.

### Task 5: S0089 GUI Memory Status

**Files:**
- Modify: `apps/gui/src/main/preload.ts` or existing bridge types if needed
- Modify: `apps/gui/src/renderer/App.tsx`
- Modify: `apps/gui/src/renderer/settings/sections/MemorySection.tsx`
- Test: `apps/gui/src/renderer/gui-shell.test.tsx`

**Steps:**
1. Load memory status when the active project changes and after memory settings load.
2. Render compact memory status rows in Settings memory section.
3. Add GUI tests for scheduled/running/failed/last-updated text.

### Task 6: Docs, Verification, Commit

**Files:**
- Modify: `docs/spec/ship/S0088-gui-streaming-thinking-contract.md`
- Modify: `docs/spec/ship/S0089-memory-reliability-and-dream-trigger.md`
- Modify: `docs/ROADMAP.md`

**Steps:**
1. Mark S0088 and S0089 Done after verification.
2. Run targeted tests first, then `pnpm typecheck && pnpm test && git diff --check`.
3. Commit with one semantic title covering both specs.
