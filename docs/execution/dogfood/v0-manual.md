## dogfood-for-v0-1th

1. new chat 需要选择 workspace 后才能创建成功；应该是我们有一个默认的 workspace，应该是我们这个软件应该有一个 自己的 workspace，用来存放chat的wotkspace 或者配置之类的一切；每次新开会话就直接使用默认的那个 workspace 同时允许通过一个选择栏选择已经添加过的 工作区

2. 涉及到工具调用，状态是 awaiting approval 但是我没看到 approval、reject、reject with reaseon 的按钮

3. 界面太丑了，需要一个 sessting 面板，或者直接可以吧页面切换为 设置；在后续功能复杂之后，就有的用了；目前就是 api key、baseurl、full access之类的

4. 界面渲染不好，需要支持 markdown 渲染

