# V3-M3: Cloud Sync

> remote synchronization for sessions, config, and state

## Goal

Allow Scorel to persist and continue across devices instead of being anchored to one local machine.

## Scope

- remote state model
- sync engine and conflict handling
- device/session continuity
- secret-aware sync boundaries

## Dependencies

- local state model stable
- collaboration and security requirements clarified enough for remote persistence
