/**
 * Token names mirroring `styles.css`. Components reference CSS variables via
 * className, so this file is kept tiny — it only holds layout sizes that
 * components need to read in JS (e.g. sidebar width).
 */

export const SIDEBAR_WIDTH = 280;
